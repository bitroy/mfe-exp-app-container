# Example environment for mfe-exp-app-container

# URL of the Todo App remoteEntry (script). Used by the runtime to load the Todo microfrontend.
REMOTE_TODO_APP_URL=http://localhost:3002/remoteEntry.js

# NODE_ENV is used by build scripts (development / production)
# NODE_ENV=development
